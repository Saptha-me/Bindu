skips:
  - B101  # Allow use of asserts (common in tests)

exclude_dirs:
  - tests  # completely exclude test directories from bandit scanning
  - examples  # exclude example scripts (may contain demo credentials)
  - .venv  # exclude virtual environment
  - venv  # exclude virtual environment

tests: []  # Include all other tests
