# Wallet Configuration
# Your Ethereum wallet private key (with 0x prefix)
# ⚠️ NEVER commit your actual private key to version control!
WALLET_PRIVATE_KEY="0x..."

# Agent Configuration
# URL of your Bindu agent (default: http://localhost:3773)
AGENT_URL="http://localhost:3773"

# Network Configuration (optional)
# The network your agent is configured to use
# Options: base-sepolia, base, ethereum, polygon, etc.
NETWORK="base-sepolia"
