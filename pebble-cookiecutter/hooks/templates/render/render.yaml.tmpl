services:
  - type: web
    name: {{project_slug}}
    runtime: python
    plan: free
    buildCommand: uv sync
    startCommand: python -m pebbling
    envVars:
      - key: PYTHONPATH
        value: .
      - key: PORT
        value: 8000
